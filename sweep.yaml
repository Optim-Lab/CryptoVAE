project: DDM
program: main.py
method: grid
# metric:
#   goal: minimize
#   name: loss
parameters:
  model:
    values: ['ExpLog']
  future:
    values: [1, 5]
  # seed:
  #   values: [1]
  # d_model:
  #   values: [8, 128]
  # d_latent:
  #   values: [4, 16]
  # prior_var:
  #   values: [0.1, 1]
  # beta:
  #   values: [0.5, 1, 2]